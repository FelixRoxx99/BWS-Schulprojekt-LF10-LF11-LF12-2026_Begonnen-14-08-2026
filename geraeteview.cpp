/* This file removed because user requested to delete geraeteview.cpp and related references.
   The original functionality is preserved in FrmMain; if you need GeraeteView later,
   restore from git history. */
